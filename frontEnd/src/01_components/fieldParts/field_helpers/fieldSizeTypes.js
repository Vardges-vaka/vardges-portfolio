export const FIELD_SIZE_TYPES = ["sm", "md", "lg"];

/** Pixel size passed to Lucide — matches CSS rem glyph box. */
export const FIELD_ICON_PX = {
  sm: 15,
  md: 18,
  lg: 20,
};

export default FIELD_SIZE_TYPES;
