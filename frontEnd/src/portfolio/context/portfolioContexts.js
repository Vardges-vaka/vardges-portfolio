import { createContext } from "react";

export const PortfolioThemeContext = createContext(null);
export const PortfolioLanguageContext = createContext(null);
export const PortfolioModeContext = createContext(null);
