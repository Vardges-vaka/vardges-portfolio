import { useMemo, useState, useCallback } from "react";

export const usePDF_image_Formatting_handlers = ({
  states,
  setters,
  api_helpers,
  translations,
}) => {
  return {
    handlers: {},
  };
};
