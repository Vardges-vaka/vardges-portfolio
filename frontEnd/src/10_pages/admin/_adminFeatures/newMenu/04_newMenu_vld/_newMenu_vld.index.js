/* ============================================================================
   04_newMenu_vld — validators (placeholder).

   When the create wizard wires to a real backend (replace the mock in
   useNewMenu_apiHelpers.js), put per-field validators here following the
   project's `{ isValid, msg, sanitizedData }` shape.
============================================================================ */
export {};
