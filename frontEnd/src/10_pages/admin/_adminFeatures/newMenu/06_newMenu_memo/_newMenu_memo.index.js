/* ============================================================================
   06_newMenu_memo — placeholder for memoised selectors / pure derivations.
   `getCompProps` already runs inside a useMemo in useNewMenu.js, so this
   folder only fills if/when heavier derivations need their own memo cache.
============================================================================ */
export {};
