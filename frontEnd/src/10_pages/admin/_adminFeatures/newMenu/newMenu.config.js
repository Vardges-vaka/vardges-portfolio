/* ============================================================================
   newMenu.config — page-local config / debug flags.
   Toggle these in development; production code reads them via _NEW_MENU_CFG.
============================================================================ */
const _NEW_MENU_CFG = {
  hooks: false,
  toastDurationMs: 2400,
  optionsPreviewCount: 4,
  factGridMin: 200,
};

export default _NEW_MENU_CFG;
