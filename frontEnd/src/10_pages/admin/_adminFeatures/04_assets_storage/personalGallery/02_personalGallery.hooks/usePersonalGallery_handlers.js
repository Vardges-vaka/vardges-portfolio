import { useMemo, useState, useCallback } from "react";

export const usePersonalGallery_handlers = ({
  states,
  setters,
  api_helpers,
  translations,
}) => {
  return {
    handlers: {},
  };
};
