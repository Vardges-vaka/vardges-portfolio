# 05_cK_menus_cnst — Constants

Feature-scoped constants — enums, defaults, section maps, key lists. Avoid magic strings anywhere else; import from here.

Barrel: `_cK_menus_cnst.index.js` (the test dir uses `cK_menus_test.index.js` — no leading underscore).
