# 02_cK_menus_hlpr — Helpers

Pure helpers — formatters, computed-field derivers, path utils, presenters. No React, no DOM. Easy to unit-test in isolation.

Barrel: `_cK_menus_hlpr.index.js` (the test dir uses `cK_menus_test.index.js` — no leading underscore).
