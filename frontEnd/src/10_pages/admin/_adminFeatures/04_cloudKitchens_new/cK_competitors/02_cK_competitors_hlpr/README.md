# 02_cK_competitors_hlpr — Helpers

Pure helpers — formatters, computed-field derivers, path utils, presenters. No React, no DOM. Easy to unit-test in isolation.

Barrel: `_cK_competitors_hlpr.index.js` (the test dir uses `cK_competitors_test.index.js` — no leading underscore).
