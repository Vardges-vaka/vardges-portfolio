# 05_cK_competitors_cnst — Constants

Feature-scoped constants — enums, defaults, section maps, key lists. Avoid magic strings anywhere else; import from here.

Barrel: `_cK_competitors_cnst.index.js` (the test dir uses `cK_competitors_test.index.js` — no leading underscore).
