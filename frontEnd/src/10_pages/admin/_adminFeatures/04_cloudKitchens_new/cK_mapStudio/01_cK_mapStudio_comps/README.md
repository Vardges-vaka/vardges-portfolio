# 01_cK_mapStudio_comps — Components

React components for the feature. Top-level page sections sit at this level; their child components go in subfolders. Exported via the barrel _cK_mapStudio_comps.index.js.

Barrel: `_cK_mapStudio_comps.index.js` (the test dir uses `cK_mapStudio_test.index.js` — no leading underscore).
