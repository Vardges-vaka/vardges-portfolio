# _styles — CSS for Marketing

CSS for the Marketing feature.

**Rule:** all colors must reference CSS variables declared in
`09_styles/lightTheme.css` and `09_styles/darkTheme.css`. Never hardcode
hex values inline (see root CLAUDE.md).

Entry file: `cK_marketing.css`.
