# 06_cK_marketing_memo — Memoization

Memoized selectors and derivations for expensive computes — list filters, sorts, aggregations. Keeps renders fast as data grows.

Barrel: `_cK_marketing_memo.index.js` (the test dir uses `cK_marketing_test.index.js` — no leading underscore).
