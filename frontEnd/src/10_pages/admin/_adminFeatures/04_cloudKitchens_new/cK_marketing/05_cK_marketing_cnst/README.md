# 05_cK_marketing_cnst — Constants

Feature-scoped constants — enums, defaults, section maps, key lists. Avoid magic strings anywhere else; import from here.

Barrel: `_cK_marketing_cnst.index.js` (the test dir uses `cK_marketing_test.index.js` — no leading underscore).
