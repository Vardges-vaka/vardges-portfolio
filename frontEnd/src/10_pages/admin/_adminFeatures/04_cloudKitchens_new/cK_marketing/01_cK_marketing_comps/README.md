# 01_cK_marketing_comps — Components

React components for the feature. Top-level page sections sit at this level; their child components go in subfolders. Exported via the barrel _cK_marketing_comps.index.js.

Barrel: `_cK_marketing_comps.index.js` (the test dir uses `cK_marketing_test.index.js` — no leading underscore).
