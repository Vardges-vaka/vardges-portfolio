# 07_cK_marketing_test — Tests

Unit + integration tests for the feature. (No test runner is wired at the project level yet — see root CLAUDE.md.)

Barrel: `_cK_marketing_test.index.js` (the test dir uses `cK_marketing_test.index.js` — no leading underscore).
