# 05_cK_kitchens_cnst — Constants

Feature-scoped constants — enums, defaults, section maps, key lists. Avoid magic strings anywhere else; import from here.

Barrel: `_cK_kitchens_cnst.index.js` (the test dir uses `cK_kitchens_test.index.js` — no leading underscore).
