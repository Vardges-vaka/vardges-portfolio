# 06_cK_kitchens_memo — Memoization

Memoized selectors and derivations for expensive computes — list filters, sorts, aggregations. Keeps renders fast as data grows.

Barrel: `_cK_kitchens_memo.index.js` (the test dir uses `cK_kitchens_test.index.js` — no leading underscore).
