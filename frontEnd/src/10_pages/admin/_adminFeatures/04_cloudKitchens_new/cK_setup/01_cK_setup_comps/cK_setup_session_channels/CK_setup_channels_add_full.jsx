import "../../_styles/cK_setup_forms/cK_setup_addForm.css";

const CK_setup_channels_add_full = ({ states, handlers, t }) => {
  return (
    <div className="cK_setup_form">
      <div className="cK_setup_form_placeholder">
        Full channel form — coming soon. Filled after the initial create via
        update.
      </div>
    </div>
  );
};

export default CK_setup_channels_add_full;
