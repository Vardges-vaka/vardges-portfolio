import "../../_styles/cK_setup_forms/cK_setup_addForm.css";

const CK_setup_channels_add_file = ({ states, handlers, t }) => {
  return (
    <div className="cK_setup_form">
      <div className="cK_setup_form_placeholder">
        File uploads — handled by a separate route, after the channel exists.
      </div>
    </div>
  );
};

export default CK_setup_channels_add_file;
