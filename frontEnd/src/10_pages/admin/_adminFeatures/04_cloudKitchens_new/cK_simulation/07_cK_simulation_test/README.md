# 07_cK_simulation_test — Tests

Unit + integration tests for the feature. (No test runner is wired at the project level yet — see root CLAUDE.md.)

Barrel: `_cK_simulation_test.index.js` (the test dir uses `cK_simulation_test.index.js` — no leading underscore).
