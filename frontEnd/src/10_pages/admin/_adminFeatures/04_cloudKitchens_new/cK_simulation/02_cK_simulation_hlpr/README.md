# 02_cK_simulation_hlpr — Helpers

Pure helpers — formatters, computed-field derivers, path utils, presenters. No React, no DOM. Easy to unit-test in isolation.

Barrel: `_cK_simulation_hlpr.index.js` (the test dir uses `cK_simulation_test.index.js` — no leading underscore).
