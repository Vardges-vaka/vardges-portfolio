# 06_cK_simulation_memo — Memoization

Memoized selectors and derivations for expensive computes — list filters, sorts, aggregations. Keeps renders fast as data grows.

Barrel: `_cK_simulation_memo.index.js` (the test dir uses `cK_simulation_test.index.js` — no leading underscore).
