# 06_cK_recipesAndStock_memo — Memoization

Memoized selectors and derivations for expensive computes — list filters, sorts, aggregations. Keeps renders fast as data grows.

Barrel: `_cK_recipesAndStock_memo.index.js` (the test dir uses `cK_recipesAndStock_test.index.js` — no leading underscore).
