# 05_cK_dashboard_cnst — Constants

Feature-scoped constants — enums, defaults, section maps, key lists. Avoid magic strings anywhere else; import from here.

Barrel: `_cK_dashboard_cnst.index.js` (the test dir uses `cK_dashboard_test.index.js` — no leading underscore).
