# 01_cK_dashboard_comps — Components

React components for the feature. Top-level page sections sit at this level; their child components go in subfolders. Exported via the barrel _cK_dashboard_comps.index.js.

Barrel: `_cK_dashboard_comps.index.js` (the test dir uses `cK_dashboard_test.index.js` — no leading underscore).
