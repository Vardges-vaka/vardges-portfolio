# 06_cK_dashboard_memo — Memoization

Memoized selectors and derivations for expensive computes — list filters, sorts, aggregations. Keeps renders fast as data grows.

Barrel: `_cK_dashboard_memo.index.js` (the test dir uses `cK_dashboard_test.index.js` — no leading underscore).
