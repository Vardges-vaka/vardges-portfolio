# 05_cK_salesAndOrders_cnst — Constants

Feature-scoped constants — enums, defaults, section maps, key lists. Avoid magic strings anywhere else; import from here.

Barrel: `_cK_salesAndOrders_cnst.index.js` (the test dir uses `cK_salesAndOrders_test.index.js` — no leading underscore).
