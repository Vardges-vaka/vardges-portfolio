# 06_cK_salesAndOrders_memo — Memoization

Memoized selectors and derivations for expensive computes — list filters, sorts, aggregations. Keeps renders fast as data grows.

Barrel: `_cK_salesAndOrders_memo.index.js` (the test dir uses `cK_salesAndOrders_test.index.js` — no leading underscore).
