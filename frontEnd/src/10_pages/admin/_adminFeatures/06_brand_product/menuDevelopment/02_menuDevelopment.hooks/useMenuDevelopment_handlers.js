import { useMemo, useState, useCallback } from "react";

export const useMenuDevelopment_handlers = ({
  states,
  setters,
  api_helpers,
  translations,
}) => {
  return {
    handlers: {},
  };
};
