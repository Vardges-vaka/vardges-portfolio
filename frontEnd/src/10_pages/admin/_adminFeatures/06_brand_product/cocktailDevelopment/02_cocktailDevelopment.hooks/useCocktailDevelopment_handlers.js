import { useMemo, useState, useCallback } from "react";

export const useCocktailDevelopment_handlers = ({
  states,
  setters,
  api_helpers,
  translations,
}) => {
  return {
    handlers: {},
  };
};
