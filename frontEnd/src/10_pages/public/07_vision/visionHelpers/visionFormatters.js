/**
 * Vision Helper Functions
 */

export const filterVisionByProfile = (visionData, currentProfile) => {
  if (!visionData) return null;
  // Vision content is available for all profiles
  return visionData;
};

