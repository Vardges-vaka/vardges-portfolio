/**
 * How many options a customer may pick from a single modifier group.
 *  - "single"   → exactly one option
 *  - "multiple" → any number of options
 */
export const MODIFIER_SELECTION_MODES = ["single", "multiple"];
